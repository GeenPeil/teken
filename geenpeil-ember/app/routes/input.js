import Ember from 'ember';

export default Ember.Route.extend({

  //TODO get form model for provided parameter
  //TODO display component (string or signature) depending on form type
  //TODO put resulting value in the form map
  //TODO handle back button
  //TODO handle submit button

  setupController : function(controller,model) {
    //TODO - setup existing value if found in the form data map
  }

});
