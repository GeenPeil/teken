import { displayFactory } from '../../../helpers/display-factory';
import { module, test } from 'qunit';

module('Unit | Helper | display factory');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = displayFactory(42);
  assert.ok(result);
});
